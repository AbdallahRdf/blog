import { createContext } from "react";

export const ThemeContext = createContext({});
export const AuthContext = createContext({});
export const SupabaseContext = createContext({});
export const PostAuthorContext = createContext({});